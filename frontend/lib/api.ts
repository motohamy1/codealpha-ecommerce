const API_URL = process.env.NEXT_PUBLIC_API_URL || "";export async function fetchProducts() {  const res = await fetch(`${API_URL}/product`);  if (!res.ok) {    throw new Error("Failed to fetch products");  }  return res.json();}export async function addToCart(userId: string, productId: string, quantity: number, unitPrice: number) {  const res = await fetch(`${API_URL}/cart/${userId}/add`, {    method: "POST",    headers: {      "Content-Type": "application/json",    },    body: JSON.stringify({ productId, unitPrice, quantity }),  });  if (!res.ok) {    throw new Error("Failed to add to cart");  }  return res.json();}
